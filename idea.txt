npm run build


deploy to vercel
npm i -g vercel
vercel --prod
Set build command: npm run build, output dir: dist.
Deploy to Render
    Push to GitHub
    New → Static Site
    Build command: npm run build
    Publish directory: dist


echo "# lumina" >> README.md
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/bibekchandsah/lumina.git
git push -u origin main


git remote add origin https://github.com/bibekchandsah/lumina.git
git branch -M main
git push -u origin main

Create a Firebase project
    Go to console.firebase.google.com
    New project 
    security > authentication > get started → Enable Authentication (Google + Email/Password providers)
    database & storage > firestore > create datablase > standard edition > next > next > start in production > create - Enable Firestore Database
    Project Settings > general > your apps → Web app → register app > copy config values


Dark glassmorphism UI with smooth Framer Motion animations
Add/remove API keys for Gemini, Groq, and Grok with model selection
Smart fallback: auto-switches keys on 429 (rate limit) or 401 (invalid), with configurable cooldown and retry
Streaming responses with live typing effect
Per-key usage stats (requests, tokens, avg latency, success rate)
Export/import keys as JSON
Preferences panel (default provider, fallback toggle, streaming, retry count, cooldown)
Keys stored in localStorage only, never sent anywhere except the AI providers directly
Run npm run dev to test locally.
npm run dev -- --port 5174


'gemini-2.5-flash'


Hover a chat → pencil and trash icons appear on the right
Click pencil (or double-click the title) → inline input opens, title becomes editable
Enter to save, Escape to cancel, or click the ✓/✗ buttons
Delete still works as before
    clicking trash shows "Delete? ✓ ✗" inline — confirm with ✓ or cancel with ✗. Moving the mouse away also cancels automatically.
Typewriter component animates each character at 6ms/char as chunks stream in — fast enough to feel snappy but slow enough to see the effect. It also handles chunk appending correctly so it doesn't restart from the beginning on each new chunk, just continues from where it left off. The blinking cursor shows while typing.
Reload no longer triggers typewriter — only messages added in the current session get animated (tracked via animatedIdsRef)
Autofocus has a 50ms delay so the DOM is ready when switching chats
Scroll-to-bottom fires on every typewriter tick via onScrollNeeded callback
add edit icon so that user can edit asked question 
add scroll to bottom and top icon 

Core
    Multi-provider AI (Gemini, Groq, Grok) with auto-fallback, retry, cooldown
    Streaming responses with typewriter effect
    Stop generation mid-response
Chat
    Sidebar toggle, create/delete/rename chats
    Pin, archive, three-dot context menu per chat
    Edit user messages (re-sends with fresh answer)
    Copy button on all messages
    Scroll to top/bottom buttons
    Auto-focus textarea
Settings
    Add/edit/remove API keys per provider
    Model selector + custom model input
    Usage stats per key
    Export/import keys as JSON
    Preferences (fallback, streaming, retry, cooldown)

🔊 Read aloud — uses Web Speech API to speak the message, turns into a stop (🔇) button while speaking
🔄 Regenerate — removes the assistant message and re-sends the preceding user question, animates the new response with typewriter effect

Regenerate scrolls to the target message bubble and locks auto-scroll off, so you can watch the typewriter effect in place
During streaming/generation, auto-scroll only fires if you're within 80px of the bottom — if you scroll up, it stops following and lets you read freely

Replaces the user message content in-place
Finds the next assistant message after it and regenerates it in-place (all messages below stay intact)
Scrolls to the assistant bubble being regenerated so you can watch the typewriter effect
If there's no assistant message after (e.g. last message), appends a new one as before


give answer from muultiple modals
(fixed) when it's answering user scroll top then it's not scrolling top

